name: CI

on: [pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x]

    steps:
      - uses: actions/checkout@v2
      - uses: actions/cache@v2
        with:
          path: |
            ~/.npm
            ~/node_modules
            ~/.cache/Cypress
          key: ${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('**/package-lock.json') }}
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      - run: npm run symlinks
      - run: npx nx run-many --target=build --all --prod
      - run: node ./dist/libs/scully/scully --tds --project=sample-blog
      - run: node ./dist/libs/scully/scully killServer --project=scully-docs
      - run: node ./dist/libs/scully/scully --project=scully-docs
      - run: npm run jest:test
      - run: npm run cypress:e2e:server
        env:
          CI: true
