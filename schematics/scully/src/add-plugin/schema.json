{
  "$schema": "http://json-schema.org/schema",
  "id": "@scullyio/init:plugin",
  "title": "Scully: Create a plugin for scully",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "add the name for the plugin",
      "x-prompt": "What name do you want to use for the plugin?"
    },
    "project": {
      "type": "string",
      "description": "add the project",
      "default": "defaultProject"
    },
    "pluginType": {
      "type": "string",
      "description": "add the plugin type",
      "x-prompt": "Select the type of plugin you want to add",
      "enum": ["render", "router", "fileHandler"]
    }
  },
  "required": ["name", "pluginType"]
}
