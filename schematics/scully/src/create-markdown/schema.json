{
  "$schema": "http://json-schema.org/schema",
  "id": "add-component",
  "title": "scully create markdown module schematic",
  "type": "object",
  "properties": {
    "routingScope": {
      "enum": ["Child", "Root"],
      "type": "string",
      "description": "The scope for the new routing module.",
      "default": "Child"
    },
    "name": {
      "type": "string",
      "description": "add the name for the folder and module",
      "x-prompt": "What should be the name for the folder and module?"
    },
    "slug": {
      "type": "string",
      "description": "add the name for the :${slug}",
      "x-prompt": "What should be the slug for the markdown?"
    }
  },
  "required": []
}
