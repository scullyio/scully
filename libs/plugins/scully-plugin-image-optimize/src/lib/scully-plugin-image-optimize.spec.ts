import { scullyPluginImageOptimize } from './scully-plugin-image-optimize';

describe('scullyPluginImageOptimize', () => {
  it('should work', () => {
    expect(scullyPluginImageOptimize()).toEqual('scully-plugin-image-optimize');
  });
});
