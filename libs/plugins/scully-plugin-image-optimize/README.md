# scully-plugin-image-optimize

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `ng test scully-plugin-image-optimize` to execute the unit tests via [Jest](https://jestjs.io).
