import { pluginsScullyPluginPlaywright } from './plugins-scully-plugin-playwright';

describe('pluginsScullyPluginPlaywright', () => {
  it('should work', () => {
    expect(pluginsScullyPluginPlaywright()).toEqual('plugins-scully-plugin-playwright');
  });
});
