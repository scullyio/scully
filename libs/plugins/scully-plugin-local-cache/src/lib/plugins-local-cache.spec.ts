import { pluginsLocalCache } from './plugins-local-cache';

describe('pluginsLocalCache', () => {
  it('should work', () => {
    expect(pluginsLocalCache()).toEqual('plugins-local-cache');
  });
});
